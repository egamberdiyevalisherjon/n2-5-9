// import styles from "./button.module.css";
import { Button as BSButton } from "react-bootstrap";

const Button = () => {
  return (
    <BSButton variant="danger" size="lg">
      Button
    </BSButton>
  );
};

export default Button;
